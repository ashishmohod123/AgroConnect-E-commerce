version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=agroconnect-production-secret-key
      - DATABASE_URL=sqlite:///./agroconnect.db
    volumes:
      - ./backend/invoices:/app/invoices
